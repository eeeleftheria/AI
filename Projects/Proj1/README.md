
Και οι τρεις αλγόριθμοι ακολουθούν τον ψευδοκώδικα των διαφανειών 
στο σετ 3 με μερικές διαφοροποιήσεις και κρατάνε όλοι ένα set με όλα 
τα εξερευνημένα states. Η διαφοροποιήσή τους βασίζεται στη διαχείριση του fringe. 

*Q1 DFS*
Ο DFS αξιοποιεί τη στοίβα του util.py αφού για την επίτευξη της αναζήτησης κατά βάθος για κάθε
state που εξερευνούμε, πρέπει να συνεχίζουμε την εξερεύνηση με τα παιδιά του. Στην στοίβα
αποθηκεύουμε tuples της μορφής (state, [actions to get there]). Kάθε φορά που εξερευνάται καινούργιο 
state, προστίθεται στο σετ των explored και αφού παραχθούν οι successors του προστίθονται και αυτοί 
με τη σειρά τους στην στοίβα, αν και μόνο αν δεν έχουν ήδη εξερευνηθεί. Με κάθε επανάληψη εξετάζεται 
το τελευταίο state που μπήκε στην στοίβα. Έτσι, όταν εξερευνούμε ένα state, το αφαιρούμε από τη στοίβα, 
προσθέτουμε στην κορυφή της στοίβας τους successors του και ο τελευταίος από αυτούς είναι κ αυτός που 
θα εξερευνηθεί αμέσως μετά.

*Q2. BFS*
Για τον BFS κρατάμε μια απλή ουρά FIFO που λειτουργεί ως το fringe. Στην ουρά αποθηκεύονται tuples της  
μορφής (state, [actions to get there]) όπως στον DFS. Η διαδικασία είναι ίδια με προηγουμένως, ωστόσο 
σε κάθε επανάληψη εξετάζεται το πρώτο στοιχείο της ουράς (αυτό που έχει προστεθεί νωρίτερα) στη συνέχεια 
το επόμενο παιδί του γονιού του κοκ μέχρι να εξερευνηθεί πλήρως το συγκεκριμένο επίπεδο και να πάμε στο επόμενο. 
Για την υλοποίηση του αλγορίθμου ακολούθησα τον ψευδοκώδικα των διαφανειών του σετ 3, ωστόσο οταν παράγεται ενα 
successive state δεν ελέγχω αν αυτό ικανοποιεί το goal state (;έτσι θα έβρισκε το goal νωρίτερα). Και αυτό επειδή 
το τεστ που τρέχει με την εντολή python autograder.py -q q2 περιέχει στη σωστή λύση των expanded states και τα 
states που προηγούνται πριν την εύρεση του goal. Αυτό καθιστά αδύνατο να γίνει ο έλεγχος κατά τη παραγωγή των 
successive states , αφού τα expanded states θα ήταν λιγότερα και το test θα αποτύγχανε.

*Q3. UCS*
Σε αντίθεση με τους δύο παραπάνω αλγορίθμους, σε αυτόν χρειάζεται να κρατάμε στην δομή μας (το fringe) και το κόστος
που αναλογεί για να φτάσουμε απο το startState εως και το state που εξετάζουμε μια δεδομένη στιγμή. Όταν, παράγουμε
τους successors του currentState, πρέπει να υπολογίσουμε το συνολικό κόστος που απαιτείται για να φτάσουμε μεχρι και
τον συγκεκριμένο successor, αθροίζοντας τα currentState -> successorState και startState->current/state. Επιπλέον, 
οταν παράγουμε τους successors πρέπει να ελέγχουμε εαν ένα state βρίσκεται ήδη στην ουρά ώστε να ενημερώσουμε το κόστος 
του εαν μπορεί να βελτιωθεί. Αυτό το πετυχαίνει εσωτερικά η συνάρτηση update της priority queue, οπότε αρκεί ο έλεγχος 
για το αν είναι ή οχι στα explored το συγκεκριμένο state. Εαν δεν υπάρχει, θα εκτελεστεί η update η οποία καλύπτει και 
την περίπτωση που το state δεν υπάρχει στην ουρά αλλά και αυτή που υπάρχει ήδη και πρέπει πιθανώς να ανανεωθεί το κόστος του.

*Q4. A* 
Για τον αλγόριθμο αυτό όρισα μια βοηθητική συνάρτηση priorityFunction η οποία λειτουργεί ως τη συνάρτηση αξιολογησης 
των διαφανειών. Δέχεται ένα item της priority queue ενός state το οποίο πλέον είναι ένα tuple που αποτελείται από το 
state, τα actions, το cost και το heuristic value. Υπολογίζει το άθροισμα current cost till state + heuristic value 
from state to goal. Η υπόλοιπη υλοποίηση είναι η ίδια ακριβώς με αυτή του UCS .

*Q5. Corners problem*
Για το συγκεκριμένο πρόβλημα επέλεξα ένα state να ορίζεται μόνο από ένα tuple της μορφής (Position, tuple of visited corners), 
αφού οι δυο αυτές πληροφορίες είναι επαρκείς για το πρόβλημα. Η μέθοδος isGoalState() ελέγχει μονάχα αν έχουν επισκεφθεί και 
οι 4 ζητούμενες γωνίες μέσω του length του tuple που τις αποθηκεύει, αν έχουν επισκεφθεί τότε το state αυτό είναι ένα goal. 
Η getSuccessors() βασίζεται στον βοηθητικό κώδικα που έχει δοθεί και ελέγχει εαν για κάθε πιθανό action, το state που λαμβάνουμε 
είναι valid (δηλ. hitsWall = false). Εαν είναι, αποθηκεύεται στη λίστα με τη μορφή του state που ορίσαμε μαζί με το action και 
το κόστος 1. 

*Q6. Corners Problem: Heuristic*
Υπολογίζω τις mahattan αποστάσεις από το current state που εξετάζουμε προς κάθε unvisited corner και τις αθροίζω. Επιστρέφω 
την συντομότερη απόσταση ως το heuristic value. Είναι παραδεκτή γιατί ο pacman θα πρέπει να καλύψει σίγουρα τουλάχιστον τόση 
απόσταση για να επισκεφθεί τη συγκεκριμένη γωνία(παραπάνω εφόσον υπάρχουν εμπόδια). Για τον έλεγχο της συνέπειας υποθέτουμε 
ότι με ένα legal action κόστους 1 μετακινείται σε ενα successor state. Δηλαδή, πρέπει να ισχύει ότι h(n) <= 1 + h(n'). 
Υπάρχουν δύο εκδοχές: 
    1) Μετά την κίνηση αυτή το κοντινότερο corner παραμένει το ίδιο, άρα η ευρετική πέφτει το πολύ κατά ένα που ισχύει. 
    2) Μετά την κίνηση αυτή, φτάνουμε στην κοντινότερη αυτή γωνία, άρα το h(n') αφορά την επόμενη κοντινότερη γωνία. 
        Άρα η ανίσωση ικανοποιείται με βεβαιότητα, αφού η απόσταση για την επόμενη κοντινότερη γωνία είναι σίγουρα μεγαλύτερη.
    3) Μετά την κίνηση, αλλάζει η κοντινότερη γωνία στο state. Στην περίπτωση αυτή πάλι διατηρείται η συνέπεια, αφού η 
        απόσταση Manhattan αλλάζει την h κατά πολύ 1 για κάθε κίνηση.

*Q7. Eating All The Dots*
Ομοίως με πριν, υπολογίζω τις manhattan αποστάσεις για κάθε συντεταγμένη όπου υπάρχει φαγητό και επιστρέφω τη μέγιστη. Είναι παραδεκτή αφού
ο pacman πρέπει σίγουρα να φτάσει μέχρι εκεί και επομένως να καλύψει τουλάχιστον τόση απόσταση ή παραπάνω λόγω εμποδίων. Είναι συνεπής, γιατί όπως στη προηγούμενη ευρετική η manhattan απόσταση μεταβάλλεται κατά πολύ 1 με κάθε κίνηση και ανάλογα με το αν το next state βρίσκεται πιο κοντά, πιο μακριά, στην ίδια απόσταση από τον πιο μακρινό στόχο η ανίσωση θα ισχύει πάντα. Στην περίπτωση που μεταβάλλεται ο πιο μακρινός στόχος η ευρετική θα αυξηθεί οπότε η ανίσωση θα ισχύει σίγουρα.

*Q8. Suboptimal Search*
Για το πρόβλημα AnyFoodSearchProblem, ο στόχος είναι να βρεθούμε σε ένα state όπου υπάρχει φαγητό, άρα αρκεί να ελέγξουμε εαν το state για το οποίο καλείται η isGoalState έχει φαγητό.

